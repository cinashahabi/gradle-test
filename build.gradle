// GRADLEFILE

description="Project for a Gradle plugin build"
task hello {
    group 'greeting'
    description 'Greets you.'
    doLast { println 'Hello! How are you doing?' }
}
task fileCheck {
    group 'plugin'
        description 'Checks if a file is present or not !'
    // Create a File object representing the folder
        def testFile = new File( "/Users/tejaswi.konduri/Desktop/Gradle/src/main/java/sampleTest/README.txt" )
        // If it doesn't exist
        if( !testFile.exists() ) {
                //Print a statement ...
                doLast { println 'README file not present!!!' }
        }
}
